import { createTheme } from "@mui/material";

const BaseTheme = createTheme({
    palette: {
        primary: {
            main: "#00b0ff",
        },
        secondary: {
            main: "#ff1744",
        },
    }
});

export default BaseTheme;